syntax = "v1"

type (
	authReq {
		Clientid string `form:"clientid"`
		Username string `json:"username"`
		Password string `json:"password"`
	}
	authResp {
		Result string `json:"result"`
	}
)

service mqtt-auth-api {
	@handler AuthHandler
	post /auth (authReq) returns (authResp)
}
